# 비밀/환경
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 로컬 개발용 SQLite (dev_cat.db 등) — 실제 사용자 레코드·비밀번호 해시가 들어 있어 커밋 금지
*.db

# 로그
*.log

# 강의 영상 업로드 (LECTURE_MEDIA_DIR)
media/

# Claude Code working dir (worktrees, jobs)
.claude/
