# ★템플릿. 실제 값은 kubectl create secret 으로 넣습니다(20-secret-backend 참조).
apiVersion: v1
kind: Secret
metadata:
  name: catchap-ai-secret
  namespace: catchap
type: Opaque
stringData:
  MYSQL_PASSWORD: "REPLACE_ME"
  CAPTCHA_BACKEND_API_KEY: "REPLACE_ME"
